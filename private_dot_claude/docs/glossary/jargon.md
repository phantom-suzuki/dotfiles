# 内輪語 → 標準語の対応表

Claude がセッション内で作った比喩と、その置き換え先を並べる。

`hooks/check-jargon.py` がこの表の「検出パターン」列を読む。そして、応答に内輪語が混ざって
いないか点検する。

収録の根拠は過去セッション 2,410 ファイルの実測値。出現回数は 2026-09-11 時点。

## 表の読み方

| 列 | 中身 |
|---|---|
| 内輪語 | 使ってはいけない語 |
| 検出パターン | hook が使う正規表現。`—` なら機械検出せず、辞書としてだけ使う |
| 標準語 | 代わりに使う語 |
| 補足 | 誤検知の除外条件、使い分け |

検出パターンに `|` は書けない。表の区切り文字と同じ記号だからである。
複数の書き方をまとめたいときは、行を分けて 1 行に 1 つずつ書く。

補足に `[block]` と書いた行は、`hooks/check-jargon.py` が応答を差し戻す。それ以外の行は記録だけを取る。
`hooks/check-question.py` は、検出パターンのある全行で `AskUserQuestion` の質問文を差し戻す。

## 並列セッションの運用（最頻出）

| 内輪語 | 検出パターン | 標準語 | 補足 |
|---|---|---|---|
| 司令塔 | `司令塔` | 統括セッション | 2,694 回。最頻出の造語 [block] |
| HQ | `\bHQ\b` | 統括セッション | 428 回。司令塔の英語略 [block] |
| 枠 | `[0-9０-９]\s*枠` | 並列セッション | 2,059 回。「5 枠」→「並列セッション 5 本」 [block] |
| 枠（体制） | `枠体制` | 並列セッション N 本の体制 | — |
| 枠（別） | `枠別` | セッション別 | — |
| 班 | `[0-9０-９]\s*班` | 担当セッション | 1,188 回。「3 班」→「担当セッション 3」 [block] |
| レーン | `レーン` | 並列作業の 1 本 | 823 回。水泳の比喩。作業の流れを指すなら「工程」 [block] |
| 波 | `第\s*[0-9０-９一二三四五六七八九十]+\s*波` | 第 N 回の実施 | 502 回。ユーザーから明示的に指摘あり |
| 波（番号付き） | `波[0-9０-９]` | 第 N 回の実施 | 「波1」「波2」の形 |
| 窓明け | `窓明け` | 作業できる時間帯の終了 | ユーザーから明示的に指摘あり |
| 窓 | `作業窓` | 作業できる時間帯 | 英語 maintenance window の直訳。日本語では定着していない |
| 段 | `第\s*[0-9０-９]+\s*段` | 第 N 工程 | 「段」で工程を数えない |
| 段（順序） | `[0-9０-９]+\s*段目` | N 番目の工程 | — |
| 門 | `門が(走|終わ)` | マージ前の最終検査 | main と合わせて全テストを回す工程。「門を起こす」→「最終検査を始める」（09-13 ユーザー指摘） |
| 全数 | `全数を回` | 全テスト | `run-all-tests.sh` の走行。件数の「全数」は別（09-13 ユーザー指摘） |
| 名札 | — | タグ / バージョン指定 | GitHub Action の `@v1` など。「動く名札」→「動くタグ」（09-13 ユーザー指摘） |
| 実体のコミット | — | コミット ID（SHA） | 固定先を指すとき。「実体で固定」→「コミット ID で固定」（09-13 ユーザー指摘） |
| 必須検査 | — | 必須の自動チェック | GitHub のブランチ保護で通過が要る status check。外部製ツール名は初出で役割を添える（09-13 ユーザー指摘） |

## 作業と成果物の比喩

| 内輪語 | 検出パターン | 標準語 | 補足 |
|---|---|---|---|
| 器 | — | 親要素 / 入れ物となる定義 | 1,755 回。CSS なら具体的なセレクタ名を書く |
| 書庫 | `書庫` | 圧縮ファイル / 保管先 | zip や tar を指すなら「圧縮ファイル」 |
| 錠 | — | 排他制御 / 順番待ちの仕組み | lock の直訳。数量の「3 錠」と衝突するため機械検出はしない |
| 受け皿 | `受け皿` | 受け側の仕組み | 何が受けるのか具体名を書く [block] |
| 束 | — | まとまり | 「Epic 単位のまとまり」のように単位を書く |
| 枝 | — | ブランチ | git の文脈では「ブランチ」と書く |
| 土台 | — | 分岐元ブランチ / 共通部分 | 何の土台かを書く |
| 進捗盤 | `進捗盤` | 進捗の一覧 | 造語。Artifact なら「進捗一覧のページ」 |
| 版積み | `版積み` | 順番を守ったマージ | 造語。PR を順に積む運用を指していた |
| 投入 | — | 取り込み / 登録 | 1,256 回。目的語を必ず書く（例:「移行データを取り込む」） |
| 検算 | `検算` | 数え直しての照合 | 1,116 回。別経路で数えて突き合わせること [block] |
| 控え | — | 記録 / 保存したデータ | 何を残したのか具体的に書く |
| 写し | `[のをはが]写し` | ミラー / コピー | 「移行元の写し」→「移行元のミラー（Git サブモジュール `legacy/www.p-a.jp`）」。「写しを進める」→「サブモジュールの指し先を新しいコミットへ更新する」。どの版（日付・コミット）かを添える。2026-09-16 と 09-17 にユーザー指摘 |
| 道具 | `道具` | ツール（一式）/ スクリプト（個別の .py）/ コマンド（CLI の入口） | 「評価の道具」「道具一式」の形で出た。IT の一般用語で書く。2026-09-14 にユーザー指摘 |
| 配信（デプロイの意味で） | — | デプロイ | dev や本番へ成果物を出す作業は「デプロイ」。「配信」は CDN・動画・メールなど別の意味と衝突する。2026-09-15 に PA 移行の報告で指摘 |

## IT 標準語を和語に置き換えた例

`easy-japanese.md` の R4（外来語は日本語に）を IT 用語にまで広げた結果の造語。標準用語をそのまま書けばよい。
出現回数は 2026-09-05 から 09-19 の `AskUserQuestion` 2,065 件での数（選択肢の文を含む）。

| 内輪語 | 検出パターン | 標準語 | 補足 |
|---|---|---|---|
| 飛ばし / 飛ばす | `飛ばし` | スキップ | 「3 本とも飛ばしに裏返す」→「3 本ともスキップに切り替える」 |
| 裏返す | `裏返` | 反転する / 切り替える | フラグや判定の値を変える意味で使われた |
| 名乗り | `名乗り` | User-Agent | 「携帯の名乗り」→「モバイルの User-Agent」 |
| 横送り | `横送り` | カルーセル | 「横送りの部品」→「カルーセル」 |
| 型紙 | `型紙` | スナップショット（比較の基準） | 「寸法の型紙 263 本」→「寸法のスナップショット 263 本」。ビジュアル回帰テストの期待値 |
| 置き場 | `置き場` | ディレクトリ / 保存先 | 35 回。「og:image の置き場」→「og:image の保存先」 |
| 合図 | `合図` | 通知 / トリガー | 30 回。「統括の合図で担当が実行」→「統括セッションの指示で担当セッションが実行」 |
| 見張り | `見張` | 監視 | 16 回。「見張りから外す」→「監視の対象から外す」 |
| 裁定 | `裁定` | 決定 / 判断 | 29 回。法律用語。「9/2 の裁定」→「9/2 の決定」 |
| 台帳 | — | 一覧 / 記録（対象名を付ける） | 271 回。「意図的変更の台帳」→「意図的変更の一覧」。資産台帳のように定着した複合語は可 |
| 帯 | — | バナー / ヘッダー | 164 回（帯域との衝突があるため機械検出しない）。「題名の帯」→「タイトルのバナー」 |
| 絵 | — | 画像 | 74 回。「絵の全数確認」→「画像の全件確認」 |
| 札 | — | バッジ / ラベル / カード | 「メガメニューの札」→ 部品の名前で書く |
| 係 | — | 担当 | 47 回。「更新係のセッション」→「更新を担当するセッション」 |
| 経路（処理の意味） | — | 接続 / 処理（具体名で書く） | 80 回。「DB への経路を開く」→「DB への接続を許可する」。ネットワークの経路（ルート）は正しい用法 |
| 守り | — | ガード（必須チェック） | 「守りが赤なら止まる」→「ガードのチェックが失敗したら止まる」 |
| 起こす（作る意味） | — | 作成する | 「一覧を起こす」→「一覧を作る」。Issue を起こす → 起票する |
| 寄せる | — | 統一する / 合わせる | 50 回。「移行元に寄せる」→「移行元に合わせる」 |

## 対象をぼかす総称語

| 内輪語 | 検出パターン | 標準語 | 補足 |
|---|---|---|---|
| leaf | `\bleaf\b` | Task Issue / 末端の Issue | スクラム運用プラグインの内部用語。ユーザー向けには使わない |
| caller | `\bcaller\b` | 環境ごとの設定ディレクトリ | Terragrunt の呼び出し側。何の設定かを添える |
| 経路 | — | 対象物の名前で書く | 963 回。「静的 HTML の生成処理」のように具体名にする |
| 母集合 | — | 集計の対象 | 数学用語の転用。何を数えたかを書く |
| 生成物 | — | 出力したファイル | 何が出たのか具体名を書く |

## 判断に迷ったときの目安

- その語を初めて聞く人が、前後の文だけで意味を取れるか
- 一般語に見えるのに、この会話だけの特殊な意味を持たせていないか
- 数を数える単位（枠 / 班 / 波 / 段）を自分で発明していないか

## 検出しない語（あえて残す）

次の語はユーザー自身が使っているか、日本の開発現場で定着している。検出も言い換えもしない。

| 語 | 理由 |
|---|---|
| 起票 | Issue を作る意味で定着している |
| 棚卸し | 全件の見直しの意味で定着している |
| 正本 | 正式な出どころを指す語としてユーザーの設定文書で使われている |
| 研磨 | `CLAUDE.md` で「表現を研磨する」として定義済み |
| worktree | git の正式な機能名 |
| 走らせる | 「テストを走らせる」は実行する意味で開発現場に定着している |
| 当てる | 「CSS を当てる」「パッチを当てる」は適用する意味で定着している |

## 育て方

ユーザーが「その言葉がわからない」と言ったら、次の 3 つを行う。

1. その場で標準語に言い換えて答え直す
2. この表に 1 行足す。検出パターンは誤検知しない形にする（一般語と衝突するなら `—`）
3. 置き換え先の標準語が用語集に無ければ、該当分野のファイルにも足す

追記先は chezmoi のソース（`~/.local/share/chezmoi/private_dot_claude/docs/glossary/`）。
追記したら `chezmoi apply` を実行する。

指摘を待たずに育てる手段として、毎週月曜に launchd が `~/.claude/state/glossary-candidates.md` へ造語の候補を書く。
`glossary-research` スキルの review で候補を判定し、造語ならこの表へ、正しい用法なら「検出しない語」へ足す。
